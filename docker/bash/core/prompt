#! /bin/bash

ARROW="--> "

function branch() {
  b=$(git branch --show-current 2> /dev/null)
  [[ ! -z $b ]] && echo "[$b]"
}

export $PS1=$'%F{blue}%~ %F{241}$(branch)%f
%(?.%F{green}$ARROW%f.%F{red}$ARROW%f)'
