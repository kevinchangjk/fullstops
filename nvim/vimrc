" run all .vim files in nvim folder
for f in split(glob('$ZSH_DOTS/nvim/*.vim'), '\n')
    exe 'source' f
endfor

" run all .lua files in nvim folder
for f in split(glob('$ZSH_DOTS/nvim/*.lua'), '\n')
    exe 'source' f
endfor
